import { sharedFeatureMyFeat } from './shared-feature-my-feat';

describe('sharedFeatureMyFeat', () => {
  it('should work', () => {
    expect(sharedFeatureMyFeat()).toEqual('shared-feature-my-feat');
  });
});
